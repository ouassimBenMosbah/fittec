import { DisplayEventsPipe } from './display-events.pipe';

describe('DisplayEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
